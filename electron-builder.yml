# electron-builder config — desktop packaging (see docs/electron_pivot_plan.md).
# Icons are auto-generated from build/icon.png (1024x1024).
appId: io.cxma.desktop
productName: CxMA
copyright: © CxMA

directories:
  output: release
  buildResources: build

# Only the built renderer + the main process are needed at runtime (the renderer
# is bundled by Vite; the main process uses only Electron/Node built-ins).
files:
  - dist/**/*
  - electron/**/*
  - package.json
asar: true

mac:
  target:
    - dmg
    - zip
  category: public.app-category.productivity
  # Signing is driven by env/secrets: set CSC_LINK (+ CSC_KEY_PASSWORD) to sign,
  # and APPLE_ID/APPLE_APP_SPECIFIC_PASSWORD/APPLE_TEAM_ID to notarize. When no
  # cert is configured, the electron:* scripts set CSC_IDENTITY_AUTO_DISCOVERY=false
  # so the build stays unsigned instead of failing. See the desktop CI workflow.

win:
  target:
    - nsis

linux:
  target:
    - AppImage

nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
