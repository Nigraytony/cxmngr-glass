# Frontend environment variables
# Configure your backend API base URL (no trailing slash)
# LOCAL Development Variables
VITE_API_BASE=
PORT=
VITE_STRIPE_PUBLISHABLE_KEY=

# LIVE Variables

# AI (optional)
OPENAI_API_KEY=
OPENAI_MODEL=

# Google Gemini (optional server fallback; project-scoped keys can be used instead)
GEMINI_API_KEY=
GEMINI_MODEL=

# Anthropic Claude (optional server fallback; project-scoped keys can be used instead)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=

#
# Auth (Microsoft Entra ID / OIDC)
# For Vue (Vite) these must be prefixed with VITE_ and configured as SPA redirect URIs:
# - http://localhost:5173/login
# - https://app.cxma.io/login
#
# Scope exposed by the API app registration (e.g. api://<api-app-client-id>/access_as_user)