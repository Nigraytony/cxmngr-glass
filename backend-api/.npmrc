# Prevent puppeteer from downloading Chromium during npm install.
# This is required in CI and any environment where headless Chrome is not needed.
# The puppeteer package is used by docsPreview.js; in CI the binary is not needed.
puppeteer_skip_download=true
